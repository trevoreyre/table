import { Meta, Preview, Story } from '@storybook/addon-docs/blocks'

<Meta title="Examples|Basic" />

# Examples

## Simple table

You can use the component to render a simple table, just like a regular HTML table.

<Preview>
  <Story id="stories-basic--simple" />
</Preview>

## Striped

Add the `striped` prop on the `Table.Body` component to get a striped table.

<Preview>
  <Story id="stories-basic--striped" />
</Preview>

## Hover

Add the `hover` prop on `Table.Body` to add a hover effect to table rows.

<Preview>
  <Story id="stories-basic--hover" />
</Preview>
