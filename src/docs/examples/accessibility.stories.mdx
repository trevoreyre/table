import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Examples|Accessibility" />

# Accessibility

## Headings

It's usually a good idea to have a `<th scope="row">` heading element on each table row, when applicable. You can use `<Table.Cell as="th" scope="row" />` for this rather than `<Table.HeadCell />`.

<!-- TODO: No blank Table.HeadCell components -->
